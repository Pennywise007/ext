{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bazel build --cxxopt='-std=c++17' --compilation_mode=dbg //...",
            "windows": {
                "command":  "bazel build --cxxopt='/std:c++17' --compilation_mode=dbg //..."
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}