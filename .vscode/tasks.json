{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bazel build --compilation_mode=dbg //...",
            "windows": {
                "command": "bazel build --strip='never' --compilation_mode=dbg //..."
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}